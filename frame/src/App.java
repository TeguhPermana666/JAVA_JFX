public class App {
    public static void main(String[] args) throws Exception {
        /*
        //Jframe adalah gui untuk menambahkan component
        JFrame frame = new JFrame();//membuat object dari kelas JFrame
        frame.setVisible(true);//membuat frame terlihat
        frame.setSize(500,500);//set dimensi x dan y dari frame
                    // x    y
        frame.setTitle("GULAG");//set judul dari frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);//exit out dari app dengan default click x pada frame
        //EXIT jika mengclick close bisa DO_NOTHING wkkwkw
        //close operation di set secara default
        frame.setResizable(false);//size pada frame ga bisa di rubah secara manual oleh user
        
        //ImageIcon image = new ImageIcon(".//res//logo.png");//create ImageIcon
        ImageIcon image = new ImageIcon("D:/Desktop Programing/jfx_latihan/frame/src/res/komunisme.png");
        frame.setIconImage(image.getImage());//change logo java to my logo

        //frame.getContentPane().setBackground(Color.red);//set atomatis background
        //frame.getContentPane().setForeground(Color.red);
        
        //set rgb value background
        frame.getContentPane().setBackground(new Color(255,50,50));
        //set hexadesimal value background
        //frame.getContentPane().setBackground(new Color(0xFFFFFF))
        */
        //Myframe frame = new Myframe();//akan muncul:
//the value of local variabel frame is not used, maka buat aja gini
        
        new Myframe();//cal the construcor from class Myframe to main


        
        
    }
}
